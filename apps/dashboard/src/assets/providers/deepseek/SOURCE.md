# DeepSeek provider icon

- Asset: the inline whale silhouette in `src/shared/ui/provider-brand-mark.tsx`
- Source: the "deepseek" glyph published by Simple Icons
  (https://simpleicons.org/icons/deepseek.svg), retrieved 2026-07-27
- License: Simple Icons publishes the glyph itself under CC0 1.0
  (https://creativecommons.org/publicdomain/zero/1.0/). The DeepSeek logo
  remains a trademark of DeepSeek (深度求索); it is used in LXE Agent solely
  to identify the DeepSeek provider and is not relicensed as part of LXE
  Agent.

The glyph is stored inline as a single filled path so it can inherit
`currentColor` — used both at compact provider-mark sizes and as the large
watermark on the DeepSeek model card. The path data is otherwise unmodified.
