两个问题：

1. 命令或子进程失败时，代码杀掉进程就走了，没把“后事”收干净
结果是 Windows 的 asyncio 在回收这些管道资源时，会报一堆难看的 closed pipe / unclosed transport 警告。

2. 浏览器常驻服务以前的启动方式是“先接住它的输出管道，读一行 ready，然后不管了”
这相当于把两根管子接到自己手里，但后面没人持续处理，设计上就容易留下资源清理问题。