OK，一個新的 SKILL
用来填写发票模板，
这个模板的位置在`data\invoice_Template\invoice_Template.xlsx`

进入这个模板，在`WS-通用发票导入模版`这个sheet里，找到下面这一行
```xlsx
货箱编号*	PO Number*	单件货箱重量(KG)*	货箱长度(CM)*	货箱宽度(CM)*	货箱高度(CM)*	产品英文品名*	产品中文品名*	产品申报单价*	单箱产品申报数量*	单位*	产品材质*	产品海关编码*	产品用途*	产品品牌*	品牌类型*	产品型号*	产品销售链接*	产品图片*	预计配送时间段*
```
我们要填写的有
```xlsx
产品英文品名*	产品中文品名*	产品申报单价*	单箱产品申报数量*	单位*	产品材质*	产品海关编码*	产品用途*	产品品牌*	品牌类型*	产品型号*   产品图片*
```
--- 
那么，根据什么来填写呢？
还记得填写报关资料用的那个备货单吗，就是那个。
而且填写这个发票模板和填写报关资料的 `申报要素` 非常相似
---
关于备货单的介绍：
    用户会发送一个 excel 文件，
    （命名类似 "4.26-SP260414001-新棱镜备货-美国（4.28）-2.xlsx"，格式大概为“日期-SP单号-其它信息-国家（又是一个日期）”，注意，重要的是中间的单号和国家，在 SKILL 中要求 AI 自己从文件名中提取这两个信息，如果拿不到就和用户说缺少关键信息）
    然后要求执行填写报关单流程。

    这个 xlsx 文件中需要的数据在第三个 sheet。
    这第三个 sheet 第一行一定是下面这样的
    ```xlsx
    日期	SKU	品名	规格型号	发货量	单价	供货商	采购订单号	采购总价	商品名称	售价	总价	单位
    ```
    具体需要脚本提取的列有这些：
    ```xlsx
    品名    规格型号    发货量  商品名称    售价    总价    单位
    ```
    不过具体提取几行还是取决于第一列有几行有效数据。
---
OK来看具体的填写方式
```xlsx
产品英文品名*	产品中文品名*
```
这两个填写备货单的`商品名称`，英文名称直接做成规则映射，比如：
表带 -> Watch Band
编织表带 -> Braided Watch Band
表壳 -> Watch Case
包装盒 -> Packaging Box
手表保护套 -> Watch Protective Case
---
```xlsx
单箱产品申报数量*	单位*
```
这两个分别对应备货单的`发货量`和`单位`

```xlsx
产品材质*	产品海关编码*
```
这两个分别的填写方式直接复制填写报关资料时填写`其他申报要素`和`海关HS编码`的逻辑代码
但是`产品材质`要加点料，也就是不上英文，格式是这样的`en/cn`，比如
`尼龙/nylon`，`纸质+塑料/paper+plastic`

```xlsx
产品申报单价*
```
这个的填写方式依赖于 `单箱产品申报数量*` 和 `产品材质*`，是这样计算的：表带 + 硅胶 0.35/个
表带 + 尼龙  0.5/个
表带 + 皮革 0.5/个
表带 + 金属（或贱金属）  1/个
表壳（不看材质）  0.35/个
包装盒（不看材质） 0.32/个
手表保护套（不看材质） 0.35/个
其它 如果以上材质都不符合，直接填入`没有该材质的计算价格方式`


```xlsx
产品品牌*	品牌类型*
```
这两个填写很简单，填`无`即可

```xlsx
产品型号*
```
这个的来源是备货单的 `SKU` 这一列

```xlsx
产品图片*
```
这个就有一点绕，我们必须要用备货单的 `SKU` 这一列去下载库存 sku 数据，可以复用现在的发送请求方式，然后提取出 `库存sku图片` 复制过来即可。
图片匹配失败时怎么办：
留空并在 JSON notice 里提示


```xlsx
产品用途*
```
根据以下对应关系填写：
表带	装饰/decorate
表壳	装饰/decorate
手表保护套	保护手表用/Used for protecting watches
包装盒	装表带表壳/Watch strap and case

---

OK，上面的可以告一段落了。
接下来来个硬家伙。

备货单里的 sku 都是已经归汇总好的库存 sku，没有具体告诉我们分几箱。
汇总是什么意思呢？就比如说有一个 msku 对吧，这个 msku 会对应一个本地 sku , 这个本地 sku 可以分两种，库存 sku 和组合 sku。其中特殊就在这个组合 sku，这个组合 sku 包含多个库存 sku。
所以汇总的意思就是，备货单只会得到各个库存 sku 具体有多少个的数据。

但是，在发票模板中，我们必须要按照装箱数据分类好这些库存 sku
幸运的是 装箱数据 中有一列是`装箱数量`，根据这个`装箱数量`我们就可以倒推出一切东西

但只有 装箱数据还不够，我们必须知道 MSKU -> 组合SKU -> 对应的一些库存sku，万幸，这个数据可以从发货单中的这两列 `MSKU	` 和 `SKU发货量` 找到

---

不幸的是，备货单中的库存 sku 不会完全对应，实际的库存 sku ，你问为什么？
财务人员，会对`规则型号`和`单价`两个都一样的库存 sku 进行合并。
`规则型号`和`单价`在哪看？备货单的第二个 sheet，在这个 sheet 的第一行可以找到下面的字段：
```xlsx
SKU	产品名称	发货量	规则型号	单价	供货商	余	单价		规则型号	发货量	单价	供货商	采购总价		供货商	深圳市卓越灏科技有限公司	
```
按顺序看，一般合并后剩下的库存 sku 都是`规则型号`和`单价`相同的库存 sku 的第一个。

也就是说被合并的一定会归零，我可以把这些被归零的数平均分配到那个剩下的sku的每一箱上

比如这个下面两个都归零了，第一个就变成了 40 个，然后这个第一个存在于 1- 5箱，本来是每箱3个的，合并后我就算他每箱 8 个

```xlsx
DX07171L04	三星7代 ultra精织尼龙两节带L码（军绿黑白织唛国旗）	15	SX1-42-01	16.28
DX07171L06	三星7代 ultra精织尼龙两节带L码（午夜蓝黑白织唛国旗）	20	SX1-42-01	16.28
DX07171L03	三星7代 ultra精织尼龙两节带L码（橙色黑白织唛国旗）	5	SX1-42-01	16.28
```