> 历史 Listing 核验记录；当前备货改用[库存/组合商品类型核验](stock-combo-type-validation.md)，不再请求 Listing。

# 全量 MSKU 与 Listing 绑定核验

从 2026-09-08 起，备货不再用马帮 Active 状态筛选商品。网页 XLSX 保留全部行；官方 Listing 不传 `pStatus` 或其他状态过滤，只用于核对本地 SKU 和普通／组合类型。

- 下载仍限定单店、单站点，网页下载 ID 与官方 `sid` 各用其原有用途。保留店名别名、完整分页和跨店铺／站点检查。
- MSKU＋ASIN 唯一匹配后核对本地 SKU；ASIN 为空必须无歧义。绑定变化、类型冲突或同 MSKU 的 ASIN 不一致仍报错。源表本地 SKU 为空时保留空值。
- 未匹配记录保留在源表和全量销量分析中，写明未核验原因，不查询它的组合和深圳库存，也不进入备货计算。`Amazon.Found.*` 不作名称例外。
- 源表追加“绑定核验结果”“是否通过绑定核验”“未通过原因”。隐藏“源数据核验信息”使用版本 2、范围 `xlsx_all`，保存本轮 Listing、源指纹、未通过记录的完整商品键及原因。下游传递核验信息；计算入口用明确标识排除未核验记录，不能将其当作普通 SKU 或仅靠空库存判定。
- MSKU、ASIN、链接三个销量汇总维度均使用全量源表。最终备货和链接备货汇总只来自符合绑定核验及现有计算条件的记录。
- 返回 `original_row_count`、`binding_verified_row_count`、`binding_unverified_row_count`，前者等于后两者之和。通过绑定核验不等于有库存或建议发货，不再返回 `active_row_count`／`excluded_row_count`。
- 历史 Active 文件不修改，也不自动迁移。新计算需重新下载，生成同轮新版销量与库存报表。核验不一致立即失败。
- 无可计算记录时不生成备货建议；仍可输出全量销量及库存异常明细。保留下载原子发布、最终报表原子发布、真实错误和现有超时。

真实模型 Skill 行为回放由用户验收，不作为本次代码合并阻塞，也不调用外部模型代测。

## YYH-US 验收（2026-09-08）

正式 CLI 连续完成店铺解析、下载、销量、库存、货件和计算，共约 279 秒。源时间 `202609081150`，未关联货件快照 `202609081155`，同轮扣减 1700 件。

| 项目 | 数量 |
| --- | ---: |
| 全状态 Listing | 7296 条／8 页 |
| XLSX 及销量分析 | 2358 行 |
| 绑定核验通过 | 2132 行 |
| 未通过（均为原表缺少本地 SKU） | 226 行 |
| 本轮未匹配 Listing | 0 行 |
| 去重普通／组合本地 SKU | 1391／637 个 |
| 展开查询深圳库存 SKU | 2194 个 |
| 通过绑定核验但缺库存 | 38 行 |
| 进入备货计算 | 2094 行 |
| 最终发货建议 | 52 行／2749 件 |
| 本轮不备货 | 2042 行 |

三个销量维度均与全量源表逐项对账；全部 2094 条输入在变更前后算法中得到相同结果。新建议数不能与旧 Active 范围的固定数量直接比较。

Mac 与 Windows 新包对同一输入生成的全部 Sheet、单元格、公式及缓存一致。Excel 16 正常打开，52 行初值重算一致；修改库存和天数、排序和筛选通过，链接汇总保持生成时快照。

定向验证 220 项通过（Mac 跳过 Windows 占用测试），Windows 232 项通过。最终 rebase 后协议生成检查、TS 边界、类型检查及 Bun 全量 1444 项通过；Python 全量 1510 项通过、2 项跳过、54 个子测试通过，另有一条旧测试仍读取已删除“空运”页。将该断言改为核对首张试算表后，所在货件快照模块 40 项全部通过；本次仅修正测试，产品代码未变化，不重复运行全量测试。

本轮可复查产物保存在主工作区 `var/validation/replenish-skill-workflow/full-scope/`，包含 `acceptance.json`、`verification.json`、`binding-exclusions.csv`、各步骤日志及独立报表。模型 Skill 回放仍由用户自行验收。
