上次开会时间是 5月22日，今天是 6月8日。 
---
我做了什么？
LXE_AGENT 的 webUI 可视化基本完成
有了这个可视化，现在可以快速从网页UI看到AGENT的对话记录、SKILL 和 TOOL 了，推广这个 agent 自动化会更加轻松一些。
LXE_AGENT 在飞书上吐 token 速度加速 5 倍甚至 3 倍
怎么解决的？在开发 webUI 可视化过程中，我不得不对底层的 session 管理和模型供应商 stream 接收做优化和格式规范，在这个过程中接收 stream 模块从原来手写版改成了 anthropic 官方开发专门用来处理的 SDK，这是第一个小优化
第二个是发现了一个机制上的缺陷。发送文本到飞书是至少 150ms 一次，这 150 ms之内应该尽可能把大模型厂商发来的`stream`变成可以发送到飞书的普通文本，但之前是一次只发一个 stream 里的文本。优化了这个，提升特别大
agent harness 框架中实现权限管理模块
亚马逊备货自动化模块 skill 完成初版
---
5月22日我列了哪些目标？
打造围绕亚马逊备货自动化的一系列 skill。目前已经实现在马帮里拿取店铺 id，然后再根据 id 获取 msku数据。
在 agent harness 框架中实现权限管理模块，目前两个 agent 都是任何人都可以访问，安全问题很大，必须要做了
完成情况如何呢？
这两个目标第一周都完成了，其中第一周做的`亚马逊备货自动化`和相关业务人员对接后收集到不少优化点。
第二周做了什么？第二周的想法是，先花 1-2 天把这个 webUI 做出来，然后根据反馈优化`亚马逊备货自动化模块 skill`。但是  webUI 制作过程中优化的东西太多，很尴尬的做了五天才做完。
---
下周的目标是什么，
把 `亚马逊备货自动化模块 skill` 的优化做完，目标是至少两个小组用上而且对效果满意
如果第一个目标很顺利，可以考虑寻找一个新的模块做新的 skill
---
总结：
完成的任务主要集中于 harness 框架的优化，业务方面只是做了 `亚马逊备货自动化模块 skill`的初版，相对较少。然后我接下来对 AGENT 自动化的设想是：
机械琐碎数据范围固定的业务流程。交给本地 agent 完成，可以自定义模型，特别省 TOKEN，也不用考虑网络问题
高品质需求或超大数据量。ONLY codex can do. 比如之前的套图生成，能直接上架的质量只有 gpt-image-2 能做出来，其它模型都不行。又或者大型数据分析，要爬n个网址，对比n个链接，还要高质量的分析结果，这也只能 gpt 或 claude 这种高级模型能做，claude 价格又太贵，那么就只有 ONLY codex can do