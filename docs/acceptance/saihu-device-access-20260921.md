# 赛狐设备鉴权验收记录（2026-09-21）

服务器已先部署，桌面 Agent 的受管赛狐 MCP 已通过无业务 Token 的真实读取。赛狐新调用已迁移，旧鉴权入口尚未关闭。

## 交付范围

- 服务器功能提交：`0377d50adf31bcc835545e3412a882f600f80d0a`；线上 release：`20260921T072159Z-0377d50adf31`。
- 客户端功能提交：`a25121c6a17a5c359f5e428faa591947d6a9adc8`；Windows 测试兼容修正：`6bd013c7`、`7efcd3cd`（只改测试）。
- 38 个显式登记的普通 HTTP 操作和 MCP 按当前设备、当前 `saihu` 权限鉴权。管理接口不放开；原生标记存在时不回退旧凭据。
- 默认和可识别历史配置使用原生头，保留启停、工具限制及超时。自定义配置保留并输出核实提示。
- 统计上传、业务 Token 整体刷新、模型凭据、网页登录、独立飞书网关均保持原流程；未生成桌面安装包。

## 隔离和源码验证

服务器在独立本机 PostgreSQL `lxe_agent_data_test` 和模拟上游下运行全量测试：**1393 passed，0 skipped**。Agent、Admin、ERP 三套前端构建成功。验证过当前设备识别、模板发布收权、设备停用、已有 MCP 会话后的下一次请求拒绝、伪造来源、管理员业务权限、Host／浏览器限制、没有旧 developer Key、新方式不回退以及旧方式兼容。

客户端 `bun run verify:source`：类型检查通过，**1636 Bun 测试通过、3 跳过；1764 Python 测试通过、2 跳过，54 subtests 通过**。桌面构建成功。全量验证后最后一次修改仅将 Windows 流取消断言改为显式捕获异常；随后重跑了全部 4 个原生通信定向测试及工作区类型检查，均通过。跳过项目不计为通过。

Python 测试结束仍出现既有的 aiohttp 未关闭会话／日志流关闭警告，退出码为 0；本轮未修改该 Python 路径。前端构建存在既有大 chunk 提示。

## 真实服务器读取

Mac 使用实际 Agent MCP SDK，无 Bearer／Cookie：

- 普通 HTTP 接口目录返回 200。
- MCP 初始化、工具列表成功，返回 137 个工具。
- 已核实的只读 `get_shop_page_list`，参数 `page_no=1,page_size=1`，上游返回 `code=0,msg=success`。

Windows `PC-20240421FADR` 在独立 acceptance worktree 验证：

- 配置迁移、MCP 管理和原生通信 **14 项测试通过**；桌面源码构建成功。
- 运行实际 `OfficialMcpConnector`，移除包含 TOKEN／API_KEY／AUTHORIZATION／COOKIE 的环境变量，调用时环境映射为空；另设不可用的 HTTP／HTTPS／ALL 代理。
- 历史配置迁移后仅含 `X-LXE-Client: cli`，没有 Bearer；保留已启用状态、只读工具选择、25 秒启动及 45 秒工具超时；再次迁移不改动配置。
- 初始化、137 项工具列表和单条店铺分页查询成功，上游 `code=0`，返回 1 行；连接正常关闭。
- 使用实际运行时连接器验收，没有驱动桌面 GUI 点选或生成安装包。

Windows Bun 1.4.2 的流取消异步 rejects 断言曾触发原生崩溃；使用 Node HTTP 测试服务并显式捕获同一异常后通过。流读取、取消、压缩错误响应验证均保留；没有跳过该测试或升级运行时。

线上只做读取，没有生产停用、收权、业务写入或飞书消息。独立飞书网关未重部署、未发消息验收；兼容性依据服务器旧鉴权回归测试，不能将其表述为新的飞书端到端验收。

## 同步与部署方式

Mac 为开发源，GitHub 为共同基线，Windows 仅验收。Windows 直接拉取 GitHub 遇到连接重置，改用已发布提交的 Git bundle，经 SSH/SCP 传输并 fast-forward，未创建独立开发历史。

部署复用了同一提交已经完成且退出码为 0 的全量测试和三套构建，执行现有 release 打包检查、生产依赖安装检查和 `apply-release.sh`，保留备份、迁移、健康检查和回滚机制，没有重复运行相同测试。部署结果为成功，服务 active。
