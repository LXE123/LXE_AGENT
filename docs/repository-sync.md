# Mac 与 Windows 仓库同步

GitHub 的 `main` 是共同基线。默认在 Mac 开发；Windows 用于平台相关开发、桌面实机验收和打包。打包机器不维护另一套长期独立的源码。

## 换机器前后

1. 开始前检查工作区、当前分支，执行 `git fetch origin`，比较本机 `main` 与 `origin/main` 的提交。工作区有改动先保留，不能覆盖。
2. 本机没有独有提交时，使用 `git merge --ff-only origin/main` 同步。已经分叉时，先在领取的 worktree 内整合双方成果，再开发。
3. 按 `AGENTS.md` 领取 worktree、测试、提交、合并和归还。每个 worktree 使用自己的依赖环境。
4. 完成后推送已验证的 `main`。另一台机器 fetch 后快进，核对完整提交 ID；交付时注明哪台机器已经同步、在哪个平台完成了哪些验证。

## 本次整合的来源

- 共同起点：`efd39316`。
- Mac：`feafb4af`，9 个独有提交，包含 Skill 页面优化、云端错误提示和马帮设备鉴权。
- Windows：`da9e81fb`，4 个独有提交，包含云端自检、公共通信模块、ERP／SKU 迁移和桌面 Skill 查询。
- 使用合并提交保留双方历史。马帮请求采用 `AsyncCloudClient`；保留无业务 Token 的回归验证，真实请求头由隔离 HTTP 测试验证。
- 桌面身份请求的错误日志按事件类型验证，不再依赖最后一条日志；同时验证身份失败时独立查询成功的 Skill 范围仍然有效。

自动合并不等于行为已经验证。合并后的完整源码检查、构建以及 Windows 实机验收都以同一份整合代码为准；本次不修改服务器、不生成桌面安装包。
